kind: PersistentVolumeClaim
apiVersion: v1
metadata:
    name: onify-elasticsearch-pvc
spec:
    storageClassName: pomppa24
    resources:
        requests:
            storage: 10Gi
    accessModes:
    - ReadWriteOnce